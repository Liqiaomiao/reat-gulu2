import * as React from 'react'
const Scroll: React.FunctionComponent = (props) => {
    let { children } = props
    return (
        < div >
            {children}
        </div >
    )
}
export default Scroll
